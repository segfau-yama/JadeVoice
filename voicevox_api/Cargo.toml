[package]
name = "voicevox_api"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
serde_json = "1"
zip = { version = "2", default-features = false, features = ["deflate"] }
tokio = { version = "1", features = ["rt", "sync"] }
